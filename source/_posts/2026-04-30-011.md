---
title: 3x-ui安装：MHSanaei版官方脚本与面板配置
categories:
  - vps工具
description: 3x-ui是x-ui目前活跃维护的版本，由MHSanaei接手开发，支持VLESS Reality、Hysteria2、MTProto等协议，新装推荐用这个版本。
abbrlink: 35791
date: 2026-04-30 10:21:52
tags:
  - 3x-ui
  - x-ui
---

3x-ui 是 [x-ui 原版](https://vpsjq.com/2026/05/08/2026-05-08-001/)（vaxilu维护）停止更新后，由 MHSanaei 接手的社区继任项目，目前仍在活跃维护，协议支持上也比原版全，加了 VLESS Reality、Hysteria2、MTProto 这些原版没有的东西，新装直接用这个版本就行。

一键安装命令：

\`\`\`bash
bash <(curl -Ls https://raw.githubusercontent.com/mhsanaei/3x-ui/master/install.sh)
\`\`\`

装完终端会直接打印面板地址、端口、用户名、密码和访问路径，记下来。用浏览器打开对应地址，默认用户名密码都是 admin，登进去第一件事把密码改了，入口在右上角用户名那里。

端口和访问路径可以在面板设置里改。端口改完要记得在服务器防火墙放行，不然面板打不开：

\`\`\`bash
ufw allow 你的端口号
\`\`\`

如果防火墙没问题还是访问不了，大概率是服务商那边的安全组没开端口，去控制面板的防火墙或者安全组手动加一条入站规则。

访问路径建议保留随机字符串，不要改成 /admin 这种，太容易被扫到。

忘记密码的话 SSH 进服务器跑 `x-ui reset` 就能重置回 admin/admin。
