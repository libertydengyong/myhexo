---
title: 3x-ui安装：MHSanaei版官方脚本与面板配置
categories:
  - vps工具
description: 3x-ui（MHSanaei维护）一键安装脚本，支持VLESS Reality、Hysteria2等新协议，安装后登录面板配置节点的完整步骤，附常见打不开面板的排查思路。
abbrlink: 35791
date: 2026-04-30 10:21:52
tags:
  - 3x-ui
  - x-ui
---

3x-ui 是 [x-ui 原版](https://vpsjq.com/2026/05/08/2026-05-08-001/)（vaxilu维护）停止更新后，由 MHSanaei 接手的社区继任项目，目前仍在活跃维护，协议支持上也比原版全，加了 [VLESS Reality](https://vpsjq.com/2026/08/27/3x-ui-vless-reality/)、[Hysteria2](https://vpsjq.com/2026/08/27/3x-ui-hysteria2/)、MTProto 这些原版没有的东西，新装直接用这个版本就行。

一键安装命令：

\`\`\`bash
bash <(curl -Ls https://raw.githubusercontent.com/mhsanaei/3x-ui/master/install.sh)
\`\`\`

装完终端会直接打印面板地址、端口、用户名、密码和访问路径，记下来。用浏览器打开对应地址，默认用户名密码都是 admin，登进去第一件事把密码改了，入口在右上角用户名那里。

端口和访问路径可以在面板设置里改。端口改完要记得在服务器防火墙放行，不然面板打不开：

\`\`\`bash
ufw allow 你的端口号
\`\`\`

这条命令执行后立刻生效，不需要额外重启或reload ufw。

如果防火墙没问题还是访问不了，大概率是服务商那边的安全组没开端口，去控制面板的防火墙或者安全组手动加一条入站规则。

面板打不开除了端口没放行，还有一种很容易被忽略的情况——**访问路径记错或者输错了**。3x-ui默认会生成一段随机字符串作为访问路径，不是简单的IP+端口就能直接打开面板，完整地址通常是"IP:端口/一串随机路径"这种格式，如果只输了IP和端口、漏了后面的路径，同样会打不开。安装时终端打印的那一整行地址建议直接复制保存，不要凭记忆手动拼。

访问路径建议保留随机字符串，不要改成 /admin 这种，太容易被扫到。

忘记密码的话 SSH 进服务器跑 `x-ui reset` 就能重置回 admin/admin。如果是忘记了访问路径而不是密码，同样可以用这个命令重置，重置后路径也会变回默认值，再重新到面板设置里改成自己想要的。

如果你在3x-ui和原来的x-ui之间还没想清楚该用哪个，可以看看 [S-UI与3x-ui的对比](https://vpsjq.com/2026/09/06/s-ui-vs-3x-ui/)，两者架构和维护现状差异不小，选型上值得先看一眼。
